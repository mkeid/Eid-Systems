import UserModel from "../models/user_model"

module.exports = {
    login(request, response, next) {
        ((callback, limit) => {
        })()
    },

    logout(request, response, next) {
        ((callback, limit) => {
        })()
    }
}
