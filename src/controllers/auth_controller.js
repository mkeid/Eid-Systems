module.exports = {
    login(request, response, error) {
        if (error) { return next(error) }

        ((callback, limit) => {
        })()
    },

    logout(request, response, error) {
        if (error) { return next(error) }

        ((callback, limit) => {
        })()
    }
}
